@model WebApplication1.Models.StudentListViewModel

<ul class="list-group">
    @foreach(var s in Model.Students)
    {
    <li class="list-group-item">Firstname : @s.Firstname</li>
        
    }
  
</ul>